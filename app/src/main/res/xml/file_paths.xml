<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App-private external pictures directory: getExternalFilesDir(Environment.DIRECTORY_PICTURES) -->
    <external-files-path
        name="images"
        path="Pictures" />

    <!-- Cache dir (used by some flows); safe to keep. -->
    <cache-path
        name="cache"
        path="." />
</paths>
