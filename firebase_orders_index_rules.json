{
  "rules": {
    "orders": {
      ".indexOn": ["driverId"],
      "$orderId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "completedOrders": {
      ".indexOn": ["driverId"],
      "$orderId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
